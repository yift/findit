# Reverse list method

The `reverse`  method is used to reverse a list.

For example:

```bash
findit -d 'name: `name` reverse files: `files.sort().reverse().map($f $f.name)`'
```

will show the names of the files in each directory in reverse order.
