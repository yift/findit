# ToUpper string method
The `toUpper`  method (also `to_upper`, `upper` or `upperCase`) is used to convert a string to it's upper case form. This can e used for case insensitive comparisons


For example:
```bash
findit -w 'content.toUpper().contains("HELLO WORLD")'
```
will show the files that has hello world in their content.

