# Length string method
The `length`  method (also `len`, `count` or `size`) is used to get the number of character in a string.


For example:
```bash
findit -w 'name.length() > 12'
```
will show the files whose name is larger than 12 characters.

