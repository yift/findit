# Reverse string method

The `reverse`  method is used to reverse a string.

For example:

```bash
findit -d 'name: `name` reverse extension: `extension.reverse()`'
```

will show the files their reverse extension.
